# P0684 path-diluted QUMOND snapshot

Frozen: 2026-08-02

## Bottom line

The first genuinely geometric radial term improved the cross-domain result.
The frozen primary still fails, but the diagnostic map contains two universal
rows that clear every numerical spent galaxy, cluster-radial, gap-closure, and
Solar-force threshold. They are not validation and cannot be promoted post
hoc; one now generates an exact no-retuning 3D topology candidate.

The path term is

\[
\eta_b={|\Phi_b|\over r g_b},
\]

and it reduces the extra QUMOND channel exponent as the baryonic field extends
over more local acceleration lengths.

The fixed primary improves P0683's cluster score from `0.285` to `0.240 dex`
and passes the reliable-three gate at `0.187 dex`, while remaining `1.034x`
fixed RAR on 131 galaxies. It misses the all-five `0.20 dex` and 75%-gap rules.

The clean diagnostic generator uses two extra spatial channels, a quartic
potential onset, inverse-square-root path survival, and `chi_t=1e-6`. It gives:

- galaxy/RAR: `1.030`;
- all-five cluster log RMS: `0.145 dex`;
- reliable-three log RMS: `0.165 dex`;
- fixed-RAR cluster gap closed: `75.09%`; and
- Solar force proxies: pass.

These are spent radial targets. They do not prove lens topology, a covariant
theory, or superiority to dark matter.

## Next hard gate

Freeze the exact generated exponent

\[
p=1+2{\chi_b^4\over\chi_b^4+(10^{-6})^4}
[\max(\eta_b,1)]^{-1/2}
\]

and implement it in the registered 3D QUMOND solver. No value may be retuned.
It must recover RXJ2129 image multiplicity, parities, critical curves, and
heldout RMS below `3 arcsec`, then remain stable across three resolutions and
fixed baryon sensitivities. Only that survivor may open the frozen external
galaxy and cluster outcomes.

## Hosted researcher model

The public service remains staged after scientific-kernel stabilization:
Vercel for the interface and authenticated gateway, FastAPI/OpenAPI for typed
system/formula/job contracts, and Cloud Run Jobs or Modal for field solvers and
lens-root searches. Researchers will be able to load named real systems,
generate seeded synthetic galaxies/clusters, submit safe unit-aware formulas,
and retrieve immutable comparator-accounted results.

See [`the API plan`](../../docs/PUBLIC_SIMULATOR_API_PLAN.md).

## Canonical evidence

- [`P0684 results`](../../docs/P0684_PATH_DILUTED_QUMOND_RESULTS.md)
- [`P0683 results`](../../docs/P0683_POTENTIAL_CHANNEL_QUMOND_RESULTS.md)
- [`P0682 deflection atlas`](../../docs/P0682_SPENT_MULTICLUSTER_DEFLECTION_ATLAS_RESULTS.md)

