# V19CP V19X2 runtime-alias remediation

V19W5 passed the complete response archive. V19X2 then stopped before validating a cell because its successor freezer named the frozen count `required_unified_cells`, while the inherited manifest loader still reads `required_completed_cells`.

V19CP adds only `runtime_authorization.required_completed_cells = 5082`, exactly equal to the existing independently verified `required_unified_cells`. It binds the pre-change config and failure report, proves the four scientific sections unchanged, and runs the byte-identical V19X2 runner. A subsequent spectral quality-gate failure is treated as a valid scientific disposition and cannot be tuned. V19BR continues only if V19X2 passes.
