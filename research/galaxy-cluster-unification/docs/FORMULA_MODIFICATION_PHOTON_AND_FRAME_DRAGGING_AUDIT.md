# Formula-modification, photon-path, and frame-dragging audit

## Bottom line

The project has not yet found a root equation.  The authoritative registry now
contains 128 scientifically distinct scored laws, 138 explicit formula/action
fragments across 123 protocols, and 36 directly overlapping published
families.  The broad empirical winner is still

\[
{g\over g_{\rm bar}}=E_{\rm RAR}
+[1-(3C^2-2C^3)]^2[\epsilon(\rho_b)^{-1}-1],
\]

but this is RAR plus a smooth density/coherence response, not a derivation from
one action.  It is close to fixed MOND on held-out SPARC data and much closer
than fixed MOND to NFW-derived cluster radial fields, yet no universal
candidate has passed raw multi-cluster image topology and positions.

## How many situations are still difficult?

There are four immediate observational problems and two later theory-wide
problems.

| Gate | Current state | What is missing |
|---|---|---|
| Held-out galaxy rotation, including morphology groups | empirically close to MOND; some independent dwarf results are better | derive the response rather than importing RAR; retain performance across disks, bulges, dwarfs, gas-rich and low-surface-brightness systems |
| Raw strong lensing in several clusters | failed as a universal theory | all image roots, correct multiplicity/caustics, centroid, ellipticity and shear direction with one frozen setting |
| Baryons to apparent-halo scale/phase transfer | current V19 causal-assembly test | predict extent, centroid and orientation from gas, galaxies, shocks and assembly time without a fitted halo radius or direction |
| Same-object matter and photon response | incomplete | one metric must predict both member/gas dynamics and lensing in the same systems |
| Local/relativistic health | analytic Solar proxies pass for some candidates, but no survivor is complete | full PPN/ephemeris likelihood, luminal tensor propagation, positive kinetic/gradient energy and a well-posed initial-value problem |
| Weak lensing and cosmology | not passed | reduced shear, magnification, time delays, structure growth and background evolution from the same constants |

Thus the concise answer is **four immediate scenarios still block the root
equation, and six when the full theory burden is counted**.  Galaxy radial
amplitude is no longer the largest obstacle.  Spatial cluster curvature and a
healthy common metric are.

## Other source and propagation details we must keep in scope

The investigation should not reduce every system to total baryonic mass.  A
relativistic source and an astronomical measurement contain several distinct
pieces of information.

| Information | Relativistic role | Why it may distinguish galaxies and clusters | Current treatment |
|---|---|---|---|
| Rest mass and internal energy | principally `T_00`; sources the gravitoelectric potentials | controls ordinary radial attraction | already present in baryon maps |
| Baryonic mass current and angular momentum | `T_0i`; sources gravitomagnetic/frame-dragging terms | coherent disks and multistream cluster mergers have very different current topology | not yet scored as a universal full-field response; added below |
| Isotropic pressure | spatial trace of `T_ij` | hot intracluster gas carries much more pressure per unit rest mass than cold disk matter | tested in V17 pressure/stress lanes; no transferable metric response yet |
| Anisotropic and multistream stress | traceless part of `T_ij` | merger shocks, member velocity dispersion and intersecting flows contain direction | current V19 thermodynamic/member phase measurement |
| Time dependence and causal history | time derivatives and retarded field propagation | two systems with the same present density can have different assembly phase | current V19 shock/clock lane |
| Density gradients, tidal field and topology | derivatives/invariants of the baryonic field | a thin disk, bulge, dwarf and porous multi-centre cluster need not redirect a nonlinear field alike | many tensor/coherence/path tests; no survivor |
| Line-of-sight structure and foregrounds | projection of source and metric | can shift apparent centroid/shear without new gravity | explicit projection covariance required before scoring |
| Plasma, dust and instrumental propagation | ordinary electromagnetic nuisance, generally chromatic | can alter flux, phase, dispersion or shape but should not imitate achromatic gravity silently | must be modeled/control-tested, never absorbed into a gravity constant |
| Photon phase, polarization and coherence | Maxwell/QED propagation on or beyond the metric | could change interference, time delay or image formation even if mass motion is unchanged | simple illusion limits failed; nonlocal proposal below remains untested |

This is also a useful first-principles decomposition: `T_00`, `T_0i`, and
`T_ij` are not arbitrary object labels.  They are the energy, current, and
stress components of one baryonic stress-energy tensor.  A promising root
equation should respond to them covariantly rather than switching between a
disk formula and a cluster formula.

## Explicit one-parameter or one-coordinate modifications

The phrase "changed one parameter" can be ambiguous.  The strict list below
includes tests in which an established or already frozen parent response was
kept and one scalar, exponent, threshold coordinate, or propagation coordinate
was changed.  Several rows still inherit constants from their parent; they are
one-knob diagnostics, not one-constant theories.

| Registry rows | Parent | Single change | Physical question | Result/lesson |
|---|---|---|---|---|
| T004, T023 | RAR/MOND | refit the acceleration scale `a0`, first to clusters and then jointly | Is the galaxy/cluster conflict merely the wrong universal transition acceleration? | A cluster retune helps clusters but is a forbidden domain switch; one joint `a0` cannot supply the missing spatial cluster structure. |
| T006 | Refracted Gravity | make the density transition `rho_c` move with baryonic acceleration | Should diffuse matter become more responsive only when the local field is weak? | Useful derived-field diagnostic, but no independent universal transfer or action. |
| T007 | Refracted Gravity / EMOND | make `rho_c` move with baryonic potential | Does global potential depth distinguish dwarfs, giants and clusters better than local density? | Galaxy residuals improve in related tests, but deep porous clusters are misclassified. |
| T008 | Refracted Gravity | make the low-density floor `epsilon_0` depend on acceleration | Can the maximum refractive boost grow smoothly outside dense systems? | Helps amplitude diagnostics but remains an inserted empirical response. |
| T021, T022 | Newtonian gravity plus an isothermal tail | add one universal tail amplitude `lambda`; then apply the frozen Solar screen | Can an added `1/r` acceleration produce flat curves/cluster strength while disappearing locally? | Radial amplitude can improve, but a spherical tail does not generate cluster caustic geometry. |
| T027, T028 | low-acceleration excess | free the power `p`, then test the MOND-like fixed value `p=1/2` | Is the inverse-distance/excess exponent the missing universal law? | It can imitate MOND-like galaxy scaling; environment transfer was not established. |
| T055-T057 | frozen screened radial law | let the transition radius scale with mass, surface density, or concentration | Do dwarf/giant differences come from where the new response turns on rather than its amplitude? | These coordinates explain some morphology dependence but did not yield a cluster-safe universal law. |
| T058-T063 | curvature/path running | vary a global running exponent or strength | Can effective gravity keep increasing gradually with distance/curvature instead of saturating at a fixed `G`? | Some cluster amplitudes improve, but galaxy and raw-lensing transfer remain incompatible. |
| T068-T070 | same curvature-power parent | make exponent `p` a function of equivalent mass, density, or shape | Can one measured baryonic property determine the falloff exponent? | No variable-exponent form passed the joint galaxy/raw-lensing gates. |
| T084, T085 | best unbounded-running lens parent | fix `p=2` or additive strength `alpha=10` | Does a single stronger curvature response repair raw lenses? | Lensing-only closures; raw positions remained poor and the terms were not derived from a metric action. |
| T090 | fixed RAR matter dynamics | add one scalar metric-slip parameter `s=5` for the Weyl potential | Can photons see more curvature than massive tracers while galaxy dynamics stay fixed? | A pure lens-only lever is not an acceptable final theory and did not solve multi-cluster topology. |
| P0554 photon sensitivity tests | frozen matter field | vary one photon/matter multiplier around 1.75 | Is the cluster gap mainly a different photon response? | It is a useful sensitivity coordinate, but a post-hoc photon multiplier is prohibited and lacks a covariant origin. |
| T093 | closed/spherical spacetime proxy | one curvature length `L` | Can a globally curved spatial cavity amplify distant gravity geometrically? | The spherical proxy can change amplitude but not transferable cluster shape; a galaxy-only `L` became the prohibited T094 switch. |
| T125-T128 | conservative radial memory | vary diffusion length/strength, or append one memory profile | Does redistribution conserve total response while moving it outward? | Some galaxy profiles improve, but the tested one-dimensional diffusion does not yet predict two-dimensional lensing curvature. |

The important negative result is consistent across these tests: changing only
an amplitude, exponent, or transition radius can often repair a radial curve,
but it rarely creates the correct two-dimensional Hessian of the lensing
potential.  That is why V19 is measuring causal, directional baryonic state.

## Formula combinations that are not one-parameter modifications

These should not be confused with the table above.

- T009-T012 combine refracted-gravity and Sigma responses by addition,
  quadrature, product, or a density gate.
- T016 combines published RAR with a squared coherence-gated refracted term.
- T064-T080 introduce tensor alignment or accumulated path variables.
- T086-T092 append member-vector or tensor lensing structure.
- T097-T124 attempt field equations or covariant carriers rather than merely
  changing one scalar.
- P0623-P0629 tested 485 density/path-response formulas and then a composite
  potential/surface-density/route law.  Those tests treated persistent path
  contributions as phenomenology, explicitly not as a QED derivation.

## What was actually tested about a photon/QED illusion?

Three simple, calculable limits were tested against observations.

| Test | Mathematical change | Result |
|---|---|---|
| Same-source Milky Way masers | additive photon-frequency velocity term, constant or low-acceleration dependent | best constant amplitude 0.218 km/s; 95% interval -5.876 to 6.027 km/s; no support for the required tens of km/s |
| SPARC propagation distance | unsaturated logarithmic growth of photon bias with source distance | -0.233 km/s per distance decade; 95% interval -7.776 to 7.299 km/s; no unsaturated accumulation |
| M33 direct angular versus H I rotation | photon spectroscopic excess relative to geometric proper motion | H I minus astrometric rotation -9.93 km/s; 95% interval -59.62 to 39.59 km/s; a 61.4 km/s illusion has probability 0.00231 under that model |

These results rule out the simple frequency-only and unsaturated-distance
versions.  They do **not** calculate the stronger proposal in which path
amplitudes preserve environment-dependent phase or coherence.

## A calculable nonlocal photon-path proposal

For a set of unresolved propagation paths `p`, write the received amplitude as

\[
\mathcal A(x_o)=\sum_p a_p\exp[i\varphi_p],
\]

and the intensity as

\[
I=\sum_p|a_p|^2+
\sum_{p\ne q}D_{pq}\,a_pa_q^*
\exp[i(\varphi_p-\varphi_q)].
\]

Standard geometric lensing is recovered when rapidly varying cross terms
average away and stationary phase selects the classical null paths.  A precise
version of the proposed new physics can be written

\[
\varphi_p=\varphi_p^{\rm GR}
+\kappa_Q\int_p \mathcal F(I_b,T_b,\nabla T_b,\ldots)\,d\lambda,
\]

\[
D_{pq}=\exp\left[-\int\Gamma_Q(p,q;I_b,T_b)\,d\lambda\right],
\]

where `I_b` denotes baryonic curvature/density/current invariants and
`kappa_Q` is universal.  Merely changing `D_pq` changes interference and
magnification but generally not geometric image locations.  Moving images
requires the new phase to alter the stationary-path equation

\[
\delta(\varphi^{\rm GR}+\Delta\varphi_Q)=0.
\]

The stronger field form is a retarded nonlocal Maxwell equation,

\[
\nabla_\nu F^{\nu\mu}(x)
+\kappa_Q\int d^4x'\sqrt{-g(x')}
K^\mu{}_{\nu}(x,x';I_b,T_b)A^\nu(x')=0.
\]

This is now mathematical enough to simulate, but it is not yet a theory.  The
kernel must be transverse to preserve gauge invariance, retarded to preserve
causality, stable, and specified without a cluster label.  It must predict
frequency, polarization, time-delay, image-position and interference effects
together.  Because it modifies photons alone, it can explain galaxy rotation
only as an observational illusion; the existing maser and M33 tests already
make the simplest versions difficult.

## A calculable baryonic frame-dragging lane

Ordinary frame dragging is already present in linearized GR.  With
`bar(h)_mn` the trace-reversed metric perturbation,

\[
\Box\bar h_{\mu\nu}=-{16\pi G\over c^4}T_{\mu\nu}.
\]

Rest density principally sources `bar(h)_00`; baryonic mass current
`j_i=rho v_i` sources `bar(h)_0i`.  The latter is the gravitomagnetic or
Lense-Thirring channel.  Massive bodies and photons both follow the resulting
metric, so this is naturally compatible with the one-metric requirement.

A one-parameter diagnostic can hold the scalar metric fixed and test

\[
\bar h_{0i}=\left[1+\kappa_J S(I_b)\right]
\bar h_{0i}^{\rm GR}[\rho_b\mathbf v_b],
\]

where `S(I_b)` is one universal high-acceleration screen and `kappa_J` is one
universal current susceptibility.  This is not yet covariant—the selective
rescaling could violate the metric constraints—so it is only a falsifiable
weak-field diagnostic.  If data favored it, the next step would be a conserved
vector/tensor action whose weak limit produces the same response.

The required inputs are baryonic mass-current maps, not just density maps:

- stellar and gas rotation fields for galaxies, including vertical structure;
- cluster member positions, masses and velocity uncertainties;
- gas bulk-flow or line-shift constraints where available; and
- projection covariances for unmeasured transverse velocities.

The primary tests are whether the same `kappa_J` improves disk rotation and
raw cluster image roots, whether its predicted curl/shear orientation matches
lensing, and whether Solar frame-dragging and PPN constraints remain satisfied.
The GR value is the mandatory zero-extra-response control.  A null result would
reject the tested susceptibility, not the existence of ordinary
Lense-Thirring gravity.

## Long-wavelength gravity mode: explicitly included, but not new

The hypothesis that gravity has an additional wavelength longer than a Solar
System has a clean mathematical meaning: the locally measured metric response
is the high-wavenumber limit, while an extra metric mode becomes coherent only
at wavelengths comparable to a universal length `L_Sigma`.  A representative
one-metric transfer is

\[
 \widetilde{\bar h}_{\mu\nu}(k)=
 \widetilde{\bar h}^{\rm GR}_{\mu\nu}(k)
 \exp\!\left[A\exp\!\left(-(kL_\Sigma)^2\right)\right].
\]

For `k L_Sigma >> 1`, as for variation across a star system when
`L_Sigma` is galactic, the exponential tends to one and local gravity is
unchanged.  For wavelengths of order `L_Sigma` or longer it tends toward
`exp(A)`, so the same baryonic source produces a larger long-scale metric
response.  Both matter and light see the resulting metric; this is not a
lens-only multiplier.  The two universal quantities are the strength `A` and
the coherence length `L_Sigma`.

### Literal wave reading of the same premise

There is a narrower version of the idea that must not be conflated with the
transfer function above. Suppose an additional, source-generated metric mode
has a characteristic spatial wavelength

\[
 k_\Sigma={2\pi\over\lambda_\Sigma},\qquad
 \delta g_{\mu\nu}\propto
 \mathcal M_{\mu\nu}\cos(k_\Sigma s+\varphi).
\]

Across a star system of diameter `D_star << lambda_Sigma`, its Taylor
expansion is

\[
 \delta g_{\mu\nu}(s+\Delta s)=
 \delta g_{\mu\nu}(s)
 +O(D_\star/\lambda_\Sigma)
 +O[(D_\star/\lambda_\Sigma)^2].
\]

The constant metric offset is locally unobservable, and a nearly uniform
first-gradient acceleration is removed in a freely falling frame. The first
intrinsically local observable is the tidal curvature, so the anomalous
Solar-System signal can be suppressed approximately as
`(D_star/lambda_Sigma)^2`, while phase and curvature changes can become
measurable across a galaxy or cluster. This supplies a physical reason for a
local GR limit; it does **not** by itself determine the wave amplitude, source,
polarization, phase, sign, or whether the large-scale response is attractive.

A viable test therefore cannot insert an arbitrary cosmic sinusoid. Its
phase, direction, and tensor polarization must be generated uniquely by the
observed baryonic stress-energy and propagated through the one physical
metric. The pending formulation target is a covariant, retarded tensor mode
with one universal wavelength whose source depends on baryonic current,
stress, overlap, or tidal orientation. It must predict the same matter and
photon metric, reduce to GR over Solar-System baselines, and be frozen before
the replacement-cluster lensing targets are opened. No result for this
literal source-generated wave model is claimed yet.

This exact concept was already made quantitative in Sigma V3B and then tested
as an isotropic wavelength-only operator in V3C.  It could separate Solar,
galaxy and cluster scales and it generated nonzero shear on a manufactured
map.  It did not reproduce the full convergence and two-shear maps across the
two spent clusters: the joint normalized Fourier error was `0.800` against a
frozen `0.500` plausibility gate, cross-cluster nonparametric transfer errors
were `0.800--0.956`, and the required transfer had low radial coherence and
negative bins.  A separate fractional long-wavelength lensing test also failed
to transfer between SMACS J0723 and SPT-CL J0615 after its apparent gain was
identified as a mass-sheet/source-plane degeneracy.

So the long-wavelength premise remains in the idea inventory, but the already
tested **linear, isotropic, wavelength-only** realization is not a surviving
root equation.  A materially new version would have to let wavelength
coherence depend covariantly on baryonic current, stress, or tidal orientation;
changing only `A`, `L_Sigma`, or a Fourier power would repeat the closed tests.

### The simplest local long-wave field, and the exact prior-art boundary

The phrase "wavelength longer than a stellar system" can be made more concrete
without inserting a cosmic sinusoid.  In the linear weak field, introduce a
retarded, low-pass copy of the conserved baryonic stress-energy,

\[
(1-L_\Sigma^2\Box)X_{\mu\nu}=T_{\mu\nu},
\qquad
\Box\bar h_{\mu\nu}=-{16\pi G\over c^4}
  (T_{\mu\nu}+\alpha_\Sigma X_{\mu\nu}).
\]

In a static Fourier mode this gives

\[
\widetilde{\bar h}_{\mu\nu}(\mathbf k)\propto
{1\over k^2}\left[1+{\alpha_\Sigma\over1+k^2L_\Sigma^2}\right]
\widetilde T_{\mu\nu}(\mathbf k).
\]

For a point source, the scalar potential and acceleration become

\[
\Phi(r)=-{GM\over r}
\left[1+\alpha_\Sigma(1-e^{-r/L_\Sigma})\right],
\]

\[
{g(r)\over g_N(r)}=
1+\alpha_\Sigma\left[
1-\left(1+{r\over L_\Sigma}\right)e^{-r/L_\Sigma}
\right].
\]

At `r << L_Sigma`,

\[
{g-g_N\over g_N}={\alpha_\Sigma\over2}
\left({r\over L_\Sigma}\right)^2+O[(r/L_\Sigma)^3],
\]

which is the precise version of the user's premise: a galactic correlation
length makes the additional *tidal* response extraordinarily small across a
planetary system, while the response can become order unity across a galaxy or
cluster.  A long wavelength alone does not screen the ordinary GR near field;
the low-pass source equation is the additional physical assumption that makes
the new component locally smooth.

#### Scale check in ordinary units

This separation of scales is numerically large enough to be meaningful.  As a
worked control, take one universal correlation length `L_Sigma = 6 kpc`, or a
literal wavelength `lambda_Sigma = 2 pi L_Sigma = 37.7 kpc`.  At a galactic
radius of `10 kpc`, the sourced low-pass response above has reached

\[
1-(1+r/L_\Sigma)e^{-r/L_\Sigma}=0.496,
\]

or about half of its eventual amplitude `alpha_Sigma`.  Across a deliberately
large `100 AU` planetary baseline, however,

\[
{1\over2}\left({100\ {\rm AU}\over6\ {\rm kpc}}\right)^2
\simeq 3.3\times10^{-15}.
\]

The literal-wave tidal scaling is similarly tiny:

\[
\left({100\ {\rm AU}\over37.7\ {\rm kpc}}\right)^2
\simeq1.7\times10^{-16}.
\]

These numbers show that one universal galactic wavelength can be effectively
constant inside a planetary system yet vary across a galaxy.  They are only a
scale-separation check, not a Solar-System proof: the coefficient, tensor
polarization, time dependence, PPN metric, and wave propagation speed must
still be derived and tested.  In particular, a constant metric offset or
uniform acceleration is not the observable; relative acceleration and tidal
curvature are.

This point-mass acceleration is **not novel**.  It is the standard Yukawa-like
STVG/MOG form `1+alpha-alpha(1+mu r)exp(-mu r)`, with `mu=1/L_Sigma`, already
represented by `STVG-MOG` in the prior-art registry.  Moffat's original STVG
paper explicitly uses this Solar-to-galaxy behavior
([primary paper](https://arxiv.org/abs/gr-qc/0506021)).  Scalar causal kernels
that simulate dark matter and their gravitomagnetic extensions also overlap
published nonlocal gravity
([modified Poisson equation](https://arxiv.org/abs/1111.4702),
[nonlocal gravitomagnetism](https://arxiv.org/abs/1908.05431)).  Renaming
`alpha`, `mu` or `L` would rediscover those families.

The materially different diagnostic is therefore tensorial and source-state
dependent:

\[
(1-L_\Sigma^2\Box)X_{\mu\nu}
=\mathcal P_{\mu\nu}{}^{\rho\sigma}[T_b,u_b,\nabla T_b]\,T^b_{\rho\sigma},
\qquad
\Box\bar h_{\mu\nu}=-{16\pi G\over c^4}
(T^b_{\mu\nu}+\alpha_\Sigma X_{\mu\nu}).
\]

`P=identity` is the mandatory MOG/nonlocal control.  A new candidate must derive
`P` from a healthy covariant action so that separated baryonic currents,
multistream stress and tidal orientation can change the two-dimensional metric
curvature without a cluster label.  It cannot assign arbitrary weights to
`T_00`, `T_0i` and `T_ij` in a chosen coordinate frame.  A current-sensitive
projector generally requires a covariantly defined timelike field or matter
frame and therefore overlaps Einstein-aether/vector-tensor prior art; a
spin-2/traceless projector must pass ghost, constraint and characteristic
gates.

In ordinary GR scaling, cluster `T_0i/T_00` is only of order `v/c ~ 10^-3` and
random/thermal spatial stress is of order `v^2/c^2`.  The tensor terms can
rotate shear but are not expected to supply a factor-several missing amplitude
without a genuine new coupling.  This is a quantitative warning, not a
dismissal: once the V19 member mass-current and gas-stress maps exist, the
identity control and one preregistered conserved tensor projector can be scored
directly on raw image topology.

### Frozen interpretation and test ladder

`L_Sigma` is a universal propagation/correlation length, not permission to fit
one wavelength to each object.  The literal sinusoidal reading and the
low-pass sourced-field reading are distinct hypotheses.  The latter supplies
the automatic Solar-System suppression above and is the cleaner first control.

1. **LW0, published control:** set `P=identity` and solve the full two-dimensional
   field with one universal pair `(alpha_Sigma,L_Sigma)`.  This is explicitly
   scored as STVG/MOG/nonlocal prior art, not as a Sigma discovery.
2. **LW1, directional diagnostic:** replace the identity only by one conserved
   tensor projector derived from baryonic current, random stress and tidal
   orientation.  Any additional coefficient counts against the project's
   five-constant total and is frozen before a holdout is opened.
3. **Solar gate:** calculate the metric and PPN observables, rather than merely
   noting that `L_Sigma` is large.  The point-source control must satisfy the
   small-baseline scaling above together with Cassini, Mercury and propagation
   constraints.
4. **Galaxy gate:** predict complete rotation curves for every morphology with
   the same constants.  A successful long-wave mode must show a common,
   baryon-derived transition near `L_Sigma`; it may not infer a separate length
   from each observed curve.
5. **Cluster gate:** use that frozen metric to predict the complete two-dimensional
   convergence, both shear components, critical-curve topology and raw image
   roots.  Matching only a radial or NFW-derived acceleration profile is
   insufficient.

The hypothesis advances only if the same universal constants pass all three
scales.  If LW0 fails, that rejects the scalar identity realization already
known from prior art.  If LW1 changes shear orientation but not the missing
amplitude, the result localizes the problem to the source/coupling rather than
disproving every possible long-wavelength gravitational mode.

## Decision rule for these additions

1. Do not alter the frozen V19H source measurement protocol.
2. After V19H produces member/gas phase covariances, construct a baryonic
   mass-current map and score the GR frame-dragging control before fitting
   `kappa_J`.
3. Preregister one universal screened `kappa_J` diagnostic and evaluate it on
   galaxies and development clusters, then freeze it before a holdout.
4. Treat the nonlocal photon kernel as a separate experimental lane.  It must
   first pass same-source astrometry/spectroscopy, chromaticity, polarization
   and time-delay tests before it can be used on cluster image positions.
5. Neither lane can be combined with the empirical RAR/RG bridge merely because
   they repair different failures.  A combination advances only if one action
   derives the scalar, current, and photon limits with the same constants.
6. Retain the long-wavelength premise only in a directional or source-state
   dependent form.  The linear isotropic wavelength-only realization has
   already failed its cross-cluster map gate and must not be recreated by
   renaming `A`, `L_Sigma`, or the Fourier exponent.
