# Sigma v17K luminal-aether pressure carrier

## Selection result

The pressure/susceptibility hypothesis can be separated from the kinetic term
that v17J falsified. A known Einstein--aether coefficient family supplies a
healthy flat-vacuum carrier with exactly luminal tensor, vector, and aether
scalar modes. Our acceleration-dependent susceptibility remains only in the
single physical matter metric.

The frozen carrier uses

\[
\boxed{
c_1=\varepsilon,\qquad
c_3=-\varepsilon,\qquad
c_4=0,\qquad
c_2={\varepsilon\over1-2\varepsilon},
\qquad \varepsilon=10^{-7}.
}
\]

This is a **carrier-selection pass**, not a complete theory pass. The
derivative-dependent matter metric must still survive a full localized,
tilted-background constraint calculation and a complete Solar solution before
any halo target is opened.

## What changed from v17H

V17H asked one Born--Infeld-shaped acceleration term to do two jobs:

1. contribute to the dynamics of the local timelike frame; and
2. define how strongly pressure couples to the additional metric potential.

Its action sign made the first job unhealthy. V17K removes that acceleration
density entirely. A standard Einstein--aether kinetic action determines the
frame (U^\mu). The frame's proper acceleration

\[
A_\mu=U^\nu\nabla_\nu U_\mu
\]

is then used only as a local covariant input to

\[
Z={c^4A_\mu A^\mu\over a_\Sigma^2},
\qquad
\chi(Z)=(1+Z)^{-1/4}.
\]

The physical metric remains

\[
\boxed{
\widetilde g_{\mu\nu}
=e^{2\alpha\chi X}
\left(g_{\mu\nu}+2\alpha\chi XU_\mu U_\nu\right).
}
\]

Thus the physical idea is unchanged: low-acceleration random stress can source
metric slip, while a highly accelerated local frame suppresses it. The carrier
responsible for evolving that frame is materially different.

## Covariant action

In the project's ((-+++)) convention, the selected action is

\[
\begin{split}
S=\int d^4x\sqrt{-g}\{&{M_{\rm Pl}^2R\over2}
-{M_{\rm Pl}^2\over2}[(\nabla X)^2+L_\Sigma^{-2}X^2]\\
&-{M_{\rm Pl}^2\over2}\left[
c_1\nabla_\mu U_\nu\nabla^\mu U^\nu
c_2(\nabla_\mu U^\mu)^2
c_3\nabla_\mu U_\nu\nabla^\nu U^\mu
-c_4A_\mu A^\mu\right]
+\lambda(U^2+1)\}
+S_m[\widetilde g,\psi].
\end{split}
\]

There is no (F_A(Z)) term in the gravitational action. The susceptibility is
a fixed function in the universal physical metric.

At (X=0), the reciprocal scalar source remains

\[
M_{\rm Pl}^2(\Box-L_\Sigma^{-2})X+\alpha\chi\mathcal J=0,
\]

where

\[
\mathcal J=T+E=3p
\]

for a comoving isotropic fluid and vanishes for cold dust. This cancellation is
local and continuous; it does not classify an object as a galaxy or cluster.

## Why the flat modes work

Define

\[
c_{13}=c_1+c_3,\qquad
c_{14}=c_1+c_4,\qquad
c_{123}=c_1+c_2+c_3.
\]

The selected family gives

\[
c_{13}=0,\qquad c_{14}=\varepsilon,\qquad c_{123}=c_2.
\]

Using the standard Einstein--aether characteristic formulas,

\[
s_2^2={1\over1-c_{13}},
\]

\[
s_1^2={2c_1-c_1^2+c_3^2\over
2c_{14}(1-c_{13})},
\]

and

\[
s_0^2={c_{123}(2-c_{14})\over
c_{14}(1-c_{13})(2+c_{13}+3c_2)},
\]

one obtains the exact identities

\[
\boxed{s_2^2=s_1^2=s_0^2=1.}
\]

This simultaneously satisfies the project's physical-cone rule and the usual
vacuum-Cherenkov requirement that gravitational modes not be slower than
ultrarelativistic matter.

The spin-1 and spin-0 energy-sign proxies reduce to

\[
2\varepsilon>0,
\qquad
\varepsilon(2-\varepsilon)>0.
\]

The executable audit verifies these relations at 801 logarithmically spaced
values of (\varepsilon), in addition to evaluating the exact frozen point.

## Standard aether PPN skeleton

For the minimally coupled standard aether action,

\[
\alpha_1=
-{8(c_3^2+c_1c_4)\over2c_1-c_1^2+c_3^2},
\]

\[
\alpha_2={\alpha_1\over2}
-{(c_1+2c_3-c_4)(2c_1+3c_2+c_3+c_4)
\over c_{123}(2-c_{14})}.
\]

The selected family gives

\[
\boxed{\alpha_1=-4\varepsilon=-4\times10^{-7},
\qquad \alpha_2=0,\qquad \gamma=\beta=1.}
\]

The frozen (|\alpha_1|) is fifteen times below the declared
(6\times10^{-6}) weak-field bound. It also has (c_{13}=0) exactly, so the
tensor speed equals the photon speed rather than merely falling within the
(10^{-15}) multimessenger bound. The Newtonian coupling shift is only

\[
{G_N\over G}-1={1\over1-\varepsilon/2}-1
\simeq5\times10^{-8}.
\]

These are not yet the PPN parameters of the **complete Sigma theory**. In the
Solar System, (X), (\chi(A^2)), and the derivative-dependent physical metric
are sourced, even if weakly. The full solution must be derived before claiming
Cassini, Mercury, or preferred-frame success.

## Prior-art boundary

Nothing about the coefficient family is new. The all-luminal special case and
its PPN behavior are part of published Einstein--aether theory. The relevant
primary analyses are [Jacobson and
Mattingly](https://arxiv.org/abs/gr-qc/0402005),
[Eling](https://arxiv.org/abs/gr-qc/0507059),
[Jacobson's review](https://arxiv.org/abs/0801.1547), and the
[post-GW170817 constraint analysis](https://arxiv.org/abs/1802.04303).
The declared weak-field preferred-frame control is from the
[planetary-precession analysis](https://arxiv.org/abs/1210.3026).

TeVeS-like physical metrics and derivative-dependent metric transformations are
also prior art: [Bekenstein](https://arxiv.org/abs/astro-ph/0403694) and
[Zumalacárregui--García-Bellido](https://arxiv.org/abs/1308.4685).

The research question that remains potentially distinctive is narrower:
whether the single pressure source (3p), modulated by the target-blind local
invariant (A^2/a_\Sigma^2), predicts galaxy dynamics and raw cluster lensing
with the same constants and one physical metric.

## Connection to apparent halo size

If the later mathematical gates pass, the weak projected diagnostic remains

\[
(1-L_\Sigma^2\nabla_\perp^2)s_\Sigma
\propto\chi(Z)\mathcal J_T,
\qquad
\Delta W\propto\alpha\chi(Z)s_\Sigma.
\]

It contains no fitted halo radius. An apparent (R_{50}), (R_{80}),
ellipticity, and offset would have to emerge from:

- the measured spatial extent of baryonic random stress;
- the three-dimensional aether acceleration map that determines (chi); and
- one common (L_\Sigma), only if independently selected.

The decisive next step is therefore not a galaxy fit. It is to localize the
derivative-dependent metric, compute the complete tilted kinetic and constraint
matrices, and prove that this healthy vacuum carrier remains healthy when the
Sigma coupling is active.
