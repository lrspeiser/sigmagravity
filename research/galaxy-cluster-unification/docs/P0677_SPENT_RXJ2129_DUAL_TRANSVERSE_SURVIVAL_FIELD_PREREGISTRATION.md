# P0677 spent RX J2129 dual-transverse survival preregistration

P0676 changed the field in the needed direction but only raised strong-lens
RMS by `15.1%`. P0677 tests the one stronger form supplied directly by the
three-dimensional geometry. There are two directions perpendicular to the
route. If each leakage channel independently retains the unrouted fraction
`1-sigma`, their joint permeability is `(1-sigma)^2`:

\[
\mathbf A_{2\perp}=\mu\left[(1-\sigma)^2\mathbf I+
\left(1-(1-\sigma)^2\right)\hat{\mathbf h}\hat{\mathbf h}\right].
\]

The route eigenvalue remains `mu`; both transverse eigenvalues become
`mu(1-sigma)^2`. The exponent two is the number of transverse spatial
dimensions, not a fitted parameter. Every other activation, source, boundary,
solver, and photon assumption is unchanged.

To avoid promoting marginal variants, the preregistered field gate now
requires at least twice scalar RMS and at least a 100% vector-field change,
alongside convergence, positivity, boundary preservation, curl control, and
zero fitted parameters. Failure retires this confinement family rather than
authorizing arbitrary fitted powers. No raw image or sealed target is scored
in P0677.
