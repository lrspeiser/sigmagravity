# P0674 spent RX J2129 compound 3D field preregistration

P0673 established that repeated coherent opportunities make the constitutive
coefficient nonperturbative in clusters while leaving the registered galaxy
maps nearly inactive. P0674 now inserts that exact, frozen coefficient into
the same absolute three-dimensional tensor-AQUAL solve used by P0671:

\[
\nabla\!\cdot\left[\mu\left({|\nabla\Phi_c|\over a_0}\right)
\left(I-\sigma_{\rm compound}\,\hat h\hat h\right)\nabla\Phi_c\right]
=4\pi G\rho_b .
\]

The source cube, simple-MOND monopole boundary, 33-cell grid, nonlinear
solver, zero-slip light rule, and strong-lensing annulus are unchanged. The
P0673 coefficient is recomputed from the physical stars and gas; no image
position enters the calculation.

P0672 failed because its tensor altered the solved deflection by only
`0.168%`, leaving every family with one root and no critical curve. Before the
new field is calculated, P0674 requires at least a `5%` compound-versus-scalar
field change. Both solves must converge below `1e-5`, preserve the shared
boundary, remain finite and positive definite, retain at least one arcsecond
of physical deflection, avoid a greater than tenfold numerical response, and
remain curl-free under the zero-slip projection.

Passing these structural gates would only authorize a separately frozen raw
topology audit on the already spent RX J2129 images. It would not establish
lensing accuracy. P0633 galaxy kinematics and P0640 lensing constraints remain
sealed throughout P0674.
