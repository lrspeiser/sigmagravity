# P0678 spent RX J2129 required-field decomposition preregistration

P0675 proves that a large compound coefficient does not generate multiple
images. P0676-P0677 prove that the two parameter-free confinement
orientations increase the absolute field by only `15-30%`. P0678 stops guessing
new tensors and asks what the successful compact-halo comparator actually adds.

The training-only compact-halo parameters are frozen in the existing raw-lens
pilot. P0678 evaluates that NIE field and its external shear on the P0674
33-by-33 physical grid, converts the P0674 scalar field to the same reference
source redshift, and decomposes the added field into:

- an eight-bin radial monopole about the fixed baryonic center;
- the remaining angular vector field and harmonics `m=1..4`;
- external shear;
- effective convergence and approximate critical-curve sign changes; and
- correlations with nine baryon-only maps: stars, gas, total density, gas
  fraction, compound activation, Newtonian acceleration, tidal path length,
  component mismatch, and the absolute scalar deflection.

The decomposition must reconstruct the halo field to relative RMS `1e-12`,
remain finite and curl-free to the frozen numerical tolerances, use all eight
training parameter rows, populate all radial bins, and report all nine
predictors. No candidate is fitted, no image root is scored, and no sealed
target is opened.

The scientific output is a target specification for the next theory: how much
radial strength, spatial extent, and angular structure a baryon-derived law
must produce. Compact-halo quantities may diagnose that requirement but may
not become inputs to a later predictive formula.
