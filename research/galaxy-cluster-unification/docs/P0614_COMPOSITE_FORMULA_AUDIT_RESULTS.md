# P0614: audit of the actual composite equation

P0613 intentionally stress-tested conservative endpoint routing by itself on
galaxy rotation. That is not the full cluster equation. The cluster calculation
uses

\[
\boldsymbol\alpha_{\rm test}
=\boldsymbol\alpha_{P0554}+\nabla\psi_{\rm endpoint},
\]

where the endpoint layer redistributes only the angular shape of the P0554
excess and has zero circular monopole. P0614 therefore recombines the saved
scores for this same composite law in every domain.

The scalar parent gives **12.592 km/s** SPARC outer RMSE, 1.217 times fixed RAR
and 1.213 times simple MOND. The endpoint layer is an exact axisymmetric null,
so it neither improves nor worsens that galaxy score. Solar behavior is also
inherited from P0554: the largest dynamic fractional change through Saturn is
`1.14e-8`, Mercury's extra precession is `-1.730 mas/century`, and all project
Solar proxies pass.

Raw cluster behavior remains the failure:

- the one root-safe four-cluster setting has 19.159 arcsec equal-system RMS;
- on the validation pair it is **1.910 times** the limited compact-halo RMS;
- on RX J2129, which did not select the endpoint formula, the same tanh-20 law
  worsens held-out RMS from **1.256 to 14.130 arcsec**, a factor of 11.25.

The defensible conclusion is that P0554 carries the galaxy result while the
endpoint operator mainly changes cluster caustic topology. The two pieces have
not unified the observables.

This audit also corrects a parsimony misconception. There are zero gravity
parameters fit per object, but P0554 contains 11 listed global scalar constants
and the endpoint layer adds four selected numbers. This is universal in the
per-object sense, not a one-parameter theory.

The next distinct simplification is to remove the independent route-strength
law. Let the already-defined scalar excess determine its own routed fraction,

\[
f_{\rm self}={\Delta_{0554}(R_{80})\over 1+\Delta_{0554}(R_{80})}.
\]

That creates a falsifiable, parameter-reducing test: the same quantity that
sets extra radial gravity also sets how much of its angular field is redirected.
