# P0591B: bounded directional extension

P0591 preferred the largest tested return radius and directional concentration but still lost to Gaussian smoothing. This disclosed diagnostic extends only those active boundaries. It does not reopen small radii, negative directions, or additional formula terms.

The global-axis arc family is closed on MACS J0416 if the extended development choice still fails to beat smoothing by 5% on both development and method-holdout means, or if it fails either holdout individually.
