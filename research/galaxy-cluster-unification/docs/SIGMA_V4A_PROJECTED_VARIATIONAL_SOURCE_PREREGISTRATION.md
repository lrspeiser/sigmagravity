# Sigma v4A projected variational Weyl-source preregistration

## Question

The three Sigma v3 scalar tidal-memory potentials failed when their scalar
action densities were integrated over compact and distributed synthetic
sources.  That test did not calculate the signed field produced by varying the
action.  Sigma v4A asks the narrower question that follows from the failure:

> Can the Euler--Lagrange source of the strongest v3 interaction move
> convergence and both shear components toward the required cluster field
> with one sign and one shared set of constants?

AS295 and PLCKG287 are already spent.  They are used only to decide whether the
mechanism deserves a covariant completion.  No untouched evidence is opened.

## Frozen projected functional

For a projected lensing potential `psi`, define the trace-free Hessian

\[
 E_{ij}=D_{ij}\psi,
 \qquad
 D_{ij}=\partial_i\partial_j-\frac12\delta_{ij}\nabla^2,
 \qquad
 \widehat E_{ij}=E_{ij}/\tau_\Sigma .
\]

The dimensionless memory is

\[
 M=(1-L_\Sigma^2\nabla^2)^{-1}\widehat E,
\]

and the v3E interaction is

\[
 V(\widehat E,M)=
 \frac{\|[\widehat E,M]\|_F^2}
 {2(1+\|\widehat E\|_F^2)(1+\|M\|_F^2)}.
\]

The frozen low-field projected functional is

\[
 F[\psi]=\int d^2x\left[
 -\frac12|\nabla\psi|^2-2\kappa_{\rm AQUAL}\psi
 +\eta_\Sigma V(\widehat E,M)
 \right].
\]

Let `P_E` and `P_M` be the analytic derivatives of `V`.  Static Helmholtz
memory is self-adjoint, so its complete tensor derivative is

\[
 P=P_E+(1-L_\Sigma^2\nabla^2)^{-1}P_M.
\]

Variation with respect to the physical projected tide adds the required
factor `1/tau_sigma` and gives

\[
 R_W=D_{ij}\left(P^{ij}/\tau_\Sigma\right),
 \qquad
 \delta\kappa=-\frac{\eta_\Sigma}{2}R_W.
\]

The minus sign is fixed by the displayed functional.  It is not selected from
the cluster residual.  The correction shear is the unique periodic E-mode
Kaiser--Squires shear generated by `delta_kappa`.

## Parameters and physical boundary

The audit permits three shared projected constants:

- `L_sigma_kpc`, the memory length;
- `tau_sigma_projected`, the projected STF-curvature scale; and
- `eta_sigma_kpc2`, the positive interaction strength.

Every value is shared by both clusters and all three map channels.  There is no
cluster center, orientation, external shear, source offset, map multiplier, or
homogeneous memory state.  The three constants correspond schematically to
the `L_sigma`, `a_sigma/L_sigma`, and interaction strength of v3E, but the
line-of-sight conversion of `tau_sigma_projected` must be derived by a later
3D covariant theory.  It cannot survive as an independent photon-only
constant.

The high-field screen is frozen to one because this is only the projected
low-field cluster limit.  No convergence-based surrogate screen is allowed.

## Frozen data and score

All maps, coordinates, channels, Fourier window, and the 18--500 kpc scoring
band are inherited from
`configs/sigma_v3c_spent_operator_inference.json`, whose SHA-256 is
`77762bf4096decf1c88a2c7131166fde48bf72840eb74b21ec6551cb82c608c5`.

The primary calculation zero-pads each field to twice its width before applying
the memory and derivative operators, then crops back to the measured field.
A factor-three padding replay is a frozen boundary-sensitivity test.

For each `L_sigma` and `tau_sigma`, the single shared nonnegative
`eta_sigma` is solved analytically.  The objective gives equal weight to both
clusters and to convergence, shear 1, and shear 2 after normalizing each
channel by its target Fourier power.  Independent one-cluster fits are used
only for the frozen cross-transfer diagnostic.

## Gates

All of the following must pass:

1. Full functional and scalar-potential manufactured directional derivatives
   agree within `1e-6` relative error.
2. The mean signed source divided by its RMS is at most `1e-10`.
3. At least 10% of scored pixels have positive source and at least 10% have
   negative source.
4. The unconstrained optimum shared amplitude is positive, and the physical
   fit therefore does not rely on changing the action sign.
5. `L_sigma` and `tau_sigma` lie more than 1% of their logarithmic search range
   away from both boundaries.
6. Joint normalized Fourier RMSE is at most `0.500`.
7. Each cluster improves by at least 20% relative to its unmodified AQUAL
   baseline, and every cluster/channel pair improves.
8. A parameter pair learned on either cluster and transferred unchanged to the
   other gives normalized RMSE at most `0.800`.
9. Repeating the complete shared fit with factor-three rather than factor-two
   padding changes joint RMSE by at most 5%.

Failure retires this exact projected source.  Its sign will not be flipped and
its thresholds will not be moved.  Passing would justify deriving the exact
3D projection, covariant clock/frame, causal memory, high-field screen, and
principal kinetic matrix.  It would not by itself validate the theory.

