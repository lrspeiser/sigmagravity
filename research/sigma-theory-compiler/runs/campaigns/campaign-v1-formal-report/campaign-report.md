# Sigma Campaign Engine report

Campaign: `CMP-55b43fa111e201988d9f2922` — state **active**.

This report identifies work-priority candidates only within the declared grammar and completed gates. It does not claim a true or uniquely best gravity theory.

## Durable accounting

- Tasks: `{'deferred': 10, 'queued': 2, 'succeeded': 188}`
- Candidates: `{'active': 2, 'deferred': 4, 'rejected': 17}`
- Hard-gate evidence: `{'pass': 132, 'reject': 22, 'unresolved': 9}`
- Database integrity: `ok`

## Leading work candidates

- `CAND-803f0f8140df5d40ababdfac` — `+(q)+(q**2)`; front `1`, hard passes `4`, remaining claims `11`.
- `CAND-72ecdf8e80cc8320cbf39b02` — `constrained_vector_flux_v1: F=+(q)+(q**2)`; front `1`, hard passes `3`, remaining claims `11`.
- `CAND-2142fc416fcdb3fe29428a0c` — `S = ∫d⁴x √(-g) [ (M_pl²/2) f(φ) R + P(φ,X) + Q(φ,X) Box(φ) + a1(φ,X) φ_{μν}φ^{μν} + a2(φ,X) (Box(φ))² ] + S_matter[ g~_{μν}, Ψ ],  where X ≡ g^{μν}∂_μφ∂_νφ, Box(φ) ≡ g^{μν}∇_μ∇_νφ, and the single universal matter metric is g~_{μν} = C(φ,X) g_{μν} + D(φ,X) ∂_μφ ∂_νφ. All matter fields Ψ (every species, uniformly) couple minimally only to g~_{μν}; C and D are universal functions of (φ,X) with no per-species or per-object parameters.`; front `None`, hard passes `3`, remaining claims `11`.
- `CAND-403e136a0bc00206f40c1c77` — `S = S_grav + S_phi + S_m, with

S_grav = ∫d^4x sqrt(-g) (M_Pl^2/2) F(phi) R

S_phi = ∫d^4x sqrt(-g) [ P(phi,X) + Q(phi,X) Box(phi) + A1(phi,X) ( phi_{mu nu} phi^{mu nu} - (Box phi)^2 ) ]

S_m = ∫d^4x sqrt(-g_tilde) L_m[ g_tilde_{mu nu}, Psi_m ],
  g_tilde_{mu nu} = Omega(phi,X) g_{mu nu} + Gamma(phi,X) phi_mu phi_nu

where phi_mu = grad_mu phi, X = g^{mu nu} phi_mu phi_nu, phi_{mu nu} = grad_mu grad_nu phi, Box phi = g^{mu nu} phi_{mu nu}. F, P, Q, A1, Omega, Gamma are free functions restricted to the bounded_grammar below (same six functions for every matter species -- no per-species or per-object parameters enter g_tilde). The A2 term of the general class-Ia DHOST Lagrangian is fixed identically to -A1 by construction (folded into the single written combination), and A3=A4=A5=0 by grammar exclusion (not generated by the basis), restricting to the minimal degenerate quadratic subclass.`; front `None`, hard passes `3`, remaining claims `11`.
- `CAND-dc1b2f46833f5a239a719fcc` — `S = integral sqrt(-g) [M_Pl^2 R/2 + K(X) + lambda(A_mu A^mu + 1)] + S_m[g,psi]`; front `None`, hard passes `3`, remaining claims `11`.

## Failure clusters

- `universal_minimal_matter_coupling` / `gradient_state`: 15
- `universal_minimal_matter_coupling` / `measured_state`: 15
- `universal_minimal_matter_coupling` / `flux`: 9
- `higher_derivative_degeneracy_declaration` / `gradient_state`: 6
- `universal_minimal_matter_coupling` / `saturation`: 6
- `higher_derivative_degeneracy_declaration` / `measured_state`: 5
- `higher_derivative_degeneracy_declaration` / `flux`: 3
- `higher_derivative_degeneracy_declaration` / `saturation`: 2
- `higher_derivative_degeneracy_declaration` / `untagged`: 1

## Interpretation

A candidate is promoted only by completing hard gates. Historical results and LLM proposals can schedule work but cannot rescue a rejection.
