Vacuum Condensation Testbed
---------------------------

Hypothesis
----------
- A stochastic vacuum condensate forms below a critical dispersion `Ïƒ_c`, injecting an order-parameter-driven enhancement `f_cond(Ïƒ_v)` into the Î£-Gravity Burr-XII kernel.

Data Status
-----------
- No observational datasets wired in; any upcoming study must state whether it uses real Ïƒ_v catalogs or toy Monte Carlo draws.

Planned Analyses
----------------
- Explore (`A`, `ell0`, `p`, `n_coh`, `sigma_c`, `alpha`, `beta`) scaling across low-Ïƒ dwarfs through high-Ïƒ ellipticals to see if a universal critical dispersion emerges.
- Map condensate activation curves to the existing Î£-Gravity Ïƒ-band phenomenology.

Result Log Template
-------------------
- When experiments start, capture them in `results_log.md` (create on first run) listing: data provenance, fitted critical scale, order parameter behaviour, and residual statistics.
