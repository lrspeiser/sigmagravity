Graviton Pairing Testbed
------------------------

Hypothesis
----------
- Non-local graviton pairing boosts gravity when a bound-pair population `n_bound(Ïƒ_v)` forms and propagates over a coherence length Î¾ that may shrink in hotter systems.

Data Status
-----------
- No real measurements linked yet; note clearly when SPARC/cluster data (real) or toy halo mocks are employed.

Planned Analyses
----------------
- Fit (`A`, `xi0`, `p`, `n_coh`, `sigma0`, `gamma_xi`) to match Î£-Gravity kernels while tracking how Î¾ scales with Ïƒ_v.
- Compare inferred Î¾ trends to empirical â„“â‚€ â‰ˆ 5 kpc findings to test consistency.

Result Log Template
-------------------
- Create `results_log.md` upon first run with columns for dataset type (real/toy), fitted parameters, and Burr-XII fit quality metrics.
